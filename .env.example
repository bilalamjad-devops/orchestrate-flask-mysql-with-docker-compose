# Not strictly required for docker-compose (values are set directly in
# docker-compose.yml for this demo), but kept for consistency with Stage 1
# and for anyone who wants to run app.py directly outside of Compose.
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=web_db
FLASK_DEBUG=false
